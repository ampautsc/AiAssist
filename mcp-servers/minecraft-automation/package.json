{
  "name": "minecraft-automation",
  "version": "1.0.0",
  "description": "MCP server for validated Minecraft UI automation",
  "type": "module",
  "main": "build/src/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "dev": "tsc --watch",
    "test": "npm run build && node build/src/tests/test-with-mocks.js",
    "test:integration": "npm run build && node build/src/tests/integration-test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
