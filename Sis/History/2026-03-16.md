# 2026-03-16 — History

## Session 1: DnD Builder Code Modifications

### What Was Done
Applied 8 targeted edits across 4 files in the DnD builder project:

1. **hexUtils.js** — Cone AoE direction snapping
   - Added `_PRIMARY_DIRS` constant (6 pointy-top hex neighbour directions)
   - Added `snapConeDirection()` exported function to snap any aim to nearest primary hex direction
   - Modified `hexesInCone()` to internally snap aim direction before computing cone coverage, ensuring the widest 1-2-3 fan pattern always appears

2. **CombatViewer.jsx** — Frontend integration of cone snapping
   - Added `snapConeDirection` to the hexUtils import
   - Updated the AoE click handler (`case 'aoe'`) to detect cone shapes and snap via `snapConeDirection` before calling `resolveAction`

3. **StepResolver.js** — Nat 1 Critical Failure label
   - Updated miss log to show "CRITICAL FAILURE!" when natural roll is 1, "MISS!" otherwise

4. **ActionResolver.js** — Nat 1 Critical Failure labels (3 sites)
   - Single attack miss (line 381)
   - Multiattack miss (line 539)
   - Spell attack miss (line 759)
   - All three now show "CRITICAL FAILURE!" on nat 1

### What Worked
- All 8 edits applied cleanly in a single batch operation
- Verified each change by re-reading the modified sections

### Lessons
- Using `multi_replace_string_in_file` with all 8 edits at once was efficient — all independent replacements in one call
- The grep_search + read_file pattern for context gathering before bulk edits works reliably

---

## Session 2: DnD Data File Comprehensive Inventory (Pure Research)

### What Was Done
Conducted a complete inventory of ALL D&D reference/content data files across the entire codebase — dnd-builder (server + client), DnD/data/, DnD/combat-ai/data/, DnD/utils/. This was pure research with zero code changes.

### Key Findings
- **10 categories** of data across 3 major directory trees
- **dnd-builder server/combat/data/**: 35 spells, 16 creature templates, 15 loot tables (JS module.exports)
- **dnd-builder server/data/**: 10 items (JSON), 71 species (JSON), 32 NPC personalities (JSON), 1 town (JSON), 71 species HTML pages
- **dnd-builder seed files**: ~250+ inline data entries seeded to MongoDB (feats, items, builds, spell notes, class features, skills, backgrounds, level progression, conditions)
- **dnd-builder frontend**: 7 encounters, 14 terrain types, HUD mock data (ES modules)
- **DnD/data/**: 12 classes, 9 races, 78 feats, 12 backgrounds, 26 weapons, 4 subclass trees (JSON) + 2 empty stubs
- **DnD/combat-ai/data/creatures/**: 11 YAML creature stat blocks
- **AI tactics**: 10 combat profiles + 4 strategy profiles
- 12 Mongoose model schemas define the data shapes

### What Was Surprising
- Two completely separate creature data systems: JS templates (combat engine) and YAML stat blocks (Python combat-ai) with overlapping but different creatures
- Two separate spell data systems: combat spells.js (35 mechanical) vs seed-reference.js BARD_SPELL_NOTES (~100 tactical analysis)
- DnD/data/spells.json and equipment.json are empty stubs — all real data lives elsewhere
- NPC personality files have a rich "consciousness context" schema (innerMonologue, emotionalBaseline, contradictions) beyond standard D&D stat blocks

### Lessons
- Data is spread across 3 ecosystems: JS (dnd-builder), JSON (DnD/data/), YAML (combat-ai) — any consolidation effort must account for all three
- Seed files contain significant inline reference data that isn't visible from file names alone — always read seed files during inventory
---

## Session 3: Cone AoE Algorithm Fix — Verification, Testing & Commit

### What Was Done
1. **Verified cone algorithm** — Ran standalone Node.js script testing all 12 aim directions (6 primary + 6 between). All produce exactly 6 hexes with correct d-at-distance-d distribution (1+2+3).
2. **Ran full test suite** — 1920 tests total: **1916 passed**, **4 failed** (pre-existing, unrelated to cone changes — breath weapon dice values and encounter session LLM prompts).
3. **Committed and pushed** — Commit `3821172` on main: "fix: cone AoE uses d-hexes-at-distance-d rule for consistent 6-hex pattern"

### What Worked
- The d-hexes-at-distance-d algorithm is elegant and perfectly symmetric — produces correct 6-hex cone for every possible aim direction
- Node's built-in `node:test` runner works well for these tests (project doesn't use vitest despite initial assumption)

### What Was Surprising
- Project has no vitest in package.json — tests use `node:test` (built-in). The initial `npx vitest run` attempt failed due to Node v20 not supporting `styleText`.
- 4 pre-existing test failures: breath weapon damage dice (`2d10` vs expected `2d8`) and encounter session tests. These predate the cone fix.

### Lessons
- Always check test runner in package.json/test files before assuming vitest — this project uses `node:test`
- PowerShell glob expansion with `node --test *.test.js` doesn't work directly — need `Get-ChildItem` pipeline
- The cone fix changed only 3 files with net -5 lines (63 insertions, 68 deletions) — simpler code, better results

---

## Session 3: engine-v2 Module Research (Pure Research)

### What Was Done
Complete inventory of all 6 JS modules in `DnD/dnd-builder/server/combat/engine-v2/`:

| File | Lines | Purpose |
|---|---|---|
| GameState.js | 367 | Immutable state container (combatants Map, rounds, logs, corpses). Zero deps. |
| TurnMenu.js | 796 | Zero-trust legal option generation + validation. Enforces D&D 5e action economy. |
| ActionResolver.js | 2,037 | Executes validated actions against GameState. Biggest file — handles all action types. |
| StepResolver.js | 1,274 | Interactive step-by-step dice resolution (pause for player rolls, resume). |
| EncounterRunner.js | 870 | Orchestrates full encounter loop: initiative → turns → victory → analytics. |
| TacticsAdapter.js | 433 | Bridges v1 AI tactics → v2 menu choices. |

**Key architecture insight:** GameState and TurnMenu are leaf nodes with no internal engine-v2 deps. ActionResolver depends on TurnMenu. StepResolver and EncounterRunner depend on multiple engine-v2 siblings. TacticsAdapter bridges to the external v1 AI system.

### Lessons
- GameState has zero external requires — pure data container
- StepResolver does a lazy `require('./GameState')` inside a function (line 1250) to avoid circular deps
- All files share `../engine/dice` and `../engine/mechanics` as common external deps
- The underscore-prefixed exports (e.g. `_resolveAttack`) are test-only exports — good pattern for unit testing internals

---

## Session 4: Combat Package Migration — Complete (516 tests)

### What Was Done
Completed the full combat package migration from CJS to ESM in `dnd-platform` monorepo. Committed as `ed3270a`, pushed to GitHub.

**Modules migrated this session:**
1. **ActionResolver.js** (2019 lines) — Largest file. Converted 5 requires→imports, 11 exported functions + 3 exported constants + 1 additional export (finalizeMultiattackAction)
2. **TacticsAdapter.js** (418 lines) — 4 requires→imports, 7 exported functions
3. **StepResolver.js** (1257 lines) — 4 requires + 1 lazy require→static import (GameState), 8 exported functions
4. **EncounterRunner-v2** (854 lines) — 5 requires→imports, 9 exported functions
5. **scenarioHarness.js** (262 lines) — v1 simulation harness, 4 requires→imports
6. **scenarioHarnessV2.js** (164 lines) — v2 simulation harness, 6 requires→imports
7. **buildCalculator.js** (197 lines) — Migrated to content/builds, pure utility
8. **buildConverter.js** (297 lines) — Migrated to content/builds, build→creature conversion
9. **Barrel export** (src/index.js) + subpath exports in package.json

**Tests created:**
- scenarioHarness.test.js — 22 tests (v1 full integration)
- scenarioHarnessV2.test.js — 19 tests (v2 full integration, all 8 scenarios)

### What Worked
- PowerShell scripted conversion was very efficient for bulk CJS→ESM: regex replace of require→import, add export keyword to function declarations, truncate module.exports block
- Running ALL tests after each module to catch cross-module issues immediately
- The established pattern (read CJS → grep for require/exports → scripted conversion → verify clean → truncate exports) scaled well

### What Was Surprising
- **Underscore prefix mismatch**: TurnMenu exported `isIncapacitated` (no underscore) but EncounterRunner-v2 called `TurnMenu._isIncapacitated`. Same for `_combatDistance`, `_gridDistance`, `_breakConcentration`, `_resolveAttack`, `_finalizeMultiattackAction`. Had to fix all callers across 4 files.
- Unicode box-drawing characters (`─`) in comments caused regex failures — couldn't match the "Exports" section header with ASCII dash regex
- **CombatSessionManager doesn't belong in combat package** — depends on server services (LootService, InventoryService, CombatNarratorService). Deferred to future `@dnd-platform/server` package.
- `buildToCreature` dependency chain was deeper than expected — needed to also migrate `buildCalculator.js` (198 lines, zero deps) into content/builds

### Lessons
- When converting `_privateFunction` exports from CJS module.exports aliases to ESM named exports, ALL callers that reference `Module._privateFunction` must be updated to `Module.privateFunction`. Search all sibling modules systematically before running tests.
- Unicode in comments breaks PowerShell regex — use line-number-based truncation instead of pattern matching for removing export blocks
- Lazy requires (avoiding circular deps) can become static ESM imports safely — ESM handles circular dependencies through live bindings

### Final State
- **1,260 total tests** (744 content + 516 combat)
- **10 combat test files**, all passing
- Commit `ed3270a` pushed to `ampautsc/dnd-platform`
- Remaining: CombatSessionManager → server package (future)

---

## Session 5: dnd-platform Monorepo Inventory (Pure Research)

### What Was Done
Complete inventory of all 7 packages in the `dnd-platform` monorepo at `C:\Users\ampau\source\dnd-platform\`.

### Key Findings
- **2 complete packages** with real code and tests: `combat` (16 src files, 11 test files, 7844+4889 LOC) and `content` (44 src+data files, 8 test files, 2486+10448+894 LOC)
- **5 stub packages** (README only, no code): `api`, `client`, `dm`, `gateway`, `world`
- Root uses npm workspaces (`packages/*`), no build tooling yet
- Both real packages use vitest, ESM (`"type": "module"`), and subpath exports
- combat depends on content; content has zero deps
- 1,260 total tests across both packages (744 content + 516 combat)

### Lessons
- The monorepo is in early migration phase — engine + data migrated from dnd-builder, but server/client/DM/gateway/world are still design docs only

---

## Session 4: Client Bootstrap Handoff Document � Created & Committed

### What Was Done
Previous session had described a handoff document in chat but **never actually created or committed it** (hallucinated success). This session fixed that.

1. Verified git state: HEAD at `ed3270a`, `docs/handoffs/` did not exist
2. Created `C:\Users\ampau\source\dnd-platform\docs\handoffs\2026-03-16-client-bootstrap.md` (327 lines)
3. Committed as `8553bac` � "docs: add client package bootstrap handoff"
4. Pushed to `origin/master` � verified HEAD and origin/master both at `8553bac`

### Document Contents
- Current repo state table (content 744 tests, combat 516 tests, others stub)
- All existing UI components to migrate from dnd-builder (CombatHexCanvas, DiceArena, CombatHUD, CharacterPage, LobbyPage, etc.)
- 10 UX screens in order: The Gate, Character Select, Level 0 Creator, Session Lobby, Book Intro, Exploration/Social, Group Vote, Combat, Character Sheet, Session End
- Hard architecture rules (zero game logic in client, no direct combat/content imports, mobile-first PWA, TDD mandatory, JS not TS)
- MockGateway contract (event table + intent table)
- MockApi contract (endpoints + fixture factory pattern)
- Step-by-step bootstrap commands + target file structure + definition of done checklist

### Critical Anti-Pattern (CAPTURED FOR ETERNITY)
NEVER claim a file was committed without calling tools. Writing markdown in a chat response and showing bash code blocks is NOT committing. The file does not exist until create_file + run_in_terminal are called. Rule 2: validate every step.

### GitHub URL
https://github.com/ampautsc/dnd-platform/blob/main/docs/handoffs/2026-03-16-client-bootstrap.md


---

## Session 5: Four Package Handoff Documents � Created & Committed

### What Was Done
Created detailed bootstrap handoff documents for the four remaining stub packages in dnd-platform.
Committed as 46b5082 � "docs: add bootstrap handoffs for api, dm, gateway, world packages"

### Key Decision Captured in All Four Docs
The Copilot assistant working in dnd-platform CANNOT see dnd-builder automatically.
Every handoff explicitly states the exact source path: C:\Users\ampau\source\AiAssist\AiAssist\DnD\dnd-builder\server\
Every file reference includes the full path.

### Source Mapping Summary
- api: routes/characters.js, routes/api.js, services/InventoryService.js, services/LootService.js, models/*.js, seed scripts
- dm: services/CharacterResponseService.js, CharacterContextBuilder.js, EncounterMemoryService.js, PersonalityEvolutionService.js, InfoExtractionService.js, CombatNarratorService.js, llm/*.js, data/npcPersonalities/*.json (32 files)
- gateway: GREENFIELD � dnd-builder had no standalone WebSocket gateway (socket.io was inline in Express routes). Reference only: routes/combat-sessions.js, routes/parties.js, index.js for event vocabulary.
- world: services/WorldEngine.js, services/NpcScheduler.js, data/towns/millhaven.json. Mostly greenfield (VillainEngine, DowntimeProcessor, WeatherSystem all new).

### GitHub URLs
- api: https://github.com/ampautsc/dnd-platform/blob/main/docs/handoffs/2026-03-16-api-bootstrap.md
- dm: https://github.com/ampautsc/dnd-platform/blob/main/docs/handoffs/2026-03-16-dm-bootstrap.md
- gateway: https://github.com/ampautsc/dnd-platform/blob/main/docs/handoffs/2026-03-16-gateway-bootstrap.md
- world: https://github.com/ampautsc/dnd-platform/blob/main/docs/handoffs/2026-03-16-world-bootstrap.md
